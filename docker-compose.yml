version: '3.8'

services:
  shwi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shwi-web
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    env_file:
      - ./backend/.env
    restart: unless-stopped
